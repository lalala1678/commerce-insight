# 项目协作约定

当前已具备 **0–6：数据审计、ETL、指标、四页看板、客户分析与经营报告；7：本地静态演示和三服务 Compose；8：求职展示材料**。真实 Olist 数据已在原生 MySQL 8.0.41 上完成两次全量导入；本地静态演示与 API 一致性见 docs/pages/VERIFICATION.md，求职材料见 docs/portfolio/。GitHub 公共仓库已建立，Pages 和容器实际运行须以远程工作流及页面访问结果验收；本机没有 Docker 命令。先阅读 README.md、docs/ROADMAP.md、docs/METRICS.md、docs/DATA_AUDIT.md 和最新验收记录，再完成用户本次指定范围。

- 按用户本次授权范围推进并报告验收结果；可选的销量预测要单独设计基线与滚动验证，不写成已实现。
- 当前页面为经营概览、商品与履约、客户分析、经营报告。新老客、观察窗复购及 RFM 已实现；两份业务案例、按需模板报告与异常提示已实现；静态版只提供六个预设范围，定时任务与预测尚未实现。
- 保留 FastAPI + Vue3 + MySQL 方向；SQLite 仅用于地基、本地入门和快速测试。
- 示例数据必须显示为 synthetic / 合成数据，不能冒充 Olist 真实分析。
- backend.etl 管理真实分析表，backend.seed 仅管理 demo_ 合成表；保持隔离。真实页面不得回退到合成数据。
- 金额使用整数分或精确小数，订单数按订单 ID 去重，避免多表关联扩大金额。
- 先独立聚合支付和评价，再关联订单。品类筛选只计命中商品金额，整单支付和履约必须明确标注；缺支付、缺送达日期和缺评价不得整单删除。
- ETL 必须先校验输入，再用事务刷新并核对；禁止通过 DROP 或 to_sql(replace) 破坏回滚能力。当前只支持单个导入任务，不并发运行两个 ETL。
- 源文件只读且核对文件指纹；质量异常须标记并采用指标对应的有效分母，不编造退款、库存或因果结论。
- 数据库只放服务端；不将凭证、原始数据、依赖目录或虚拟环境提交 Git。
- API 查询在一个数据库读取快照内返回一致结果；接口与页面共用筛选口径，处理空集合、日期边界、缺失和失败状态。
- 指标口径变化需同时更新文档、SQL 和有效测试。
- 每步先说明修改范围，再实现、运行相关检查；不要凭未运行的测试宣称通过。
- 保持初学者可读的结构；不提前引入 Redis、消息队列、LLM 接口或微服务。
- 模型由用户在界面中选择；docs/ROADMAP.md 的建议不代表已自动切换模型。

- 客户指标遵守 docs/CUSTOMER_ANALYSIS.md：首次购买在维度筛选前、历史截止日前计算；RFM 仅覆盖窗口购买客户，阈值版本公开，不强行频次五等分。

- 报告遵守 docs/REPORTS.md：异常基线只含严格早于目标日的8个同星期日；覆盖或频次不足不判异常。历史最终状态不能称作实时预警回测。报告比较和格式导出保持同一快照，建议与事实分开。
